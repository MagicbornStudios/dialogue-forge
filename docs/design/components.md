# Components

Component library and UI primitives. For reimplementation: which shared components exist and how they are used across Forge, Writer, and other domains.

(To be populated from packages/shared and domain component usage.)
