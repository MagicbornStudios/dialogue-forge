legacy-peer-deps=true

# Private registry for @forge/* and @twick/* (Verdaccio; run pnpm registry:start in forge-agent repo)
@forge:registry=http://localhost:4873
@twick:registry=http://localhost:4873
