# =============================================================================
# Dialogue Forge - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and contains your actual secrets
# =============================================================================

# -----------------------------------------------------------------------------
# OpenRouter API Configuration
# -----------------------------------------------------------------------------
# Required for AI features (CopilotKit, Writer, Forge AI assistance)
# Get your API key at: https://openrouter.ai/
OPENROUTER_API_KEY=

# Optional: Override OpenRouter base URL (default: https://openrouter.ai/api/v1)
# OPENROUTER_BASE_URL=

# Optional: Model selection for different use cases
# AI_MODEL_CHEAP=openai/gpt-4o-mini          # Fast, cheap model for simple tasks
# AI_MODEL_DEFAULT=openai/gpt-4o             # Default model for general use
# AI_MODEL_REASONING=openai/o1-mini          # Reasoning model for complex tasks

# -----------------------------------------------------------------------------
# AI Runtime Configuration
# -----------------------------------------------------------------------------
# Temperature for AI responses (0.0-2.0, default: 0.7)
# Lower = more focused, Higher = more creative
AI_TEMPERATURE=0.7

# Maximum tokens in AI response (default: 1024)
AI_MAX_OUTPUT_TOKENS=1024

# Request timeout in milliseconds (default: 60000 = 60 seconds)
AI_REQUEST_TIMEOUT_MS=60000

# Enable/disable AI server apply functionality (default: false)
# When enabled, AI can directly apply changes to your codebase
AI_SERVER_APPLY_ENABLED=false

# -----------------------------------------------------------------------------
# OpenCode Integration
# -----------------------------------------------------------------------------
# Development: URL to OpenCode UI dev server for hot reload
# Set this when running: npm run vendor:opencode:dev
# Leave empty or unset to use production static build from public/vendor/opencode
NEXT_PUBLIC_OPENCODE_UI_DEV_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# PayloadCMS Configuration
# -----------------------------------------------------------------------------
# Server URL for PayloadCMS (default: http://localhost:3000)
# Used for API endpoints and admin panel
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Secret key for PayloadCMS encryption (REQUIRED in production)
# Generate a secure random string for production use
# Default in dev: 'dev-secret-change-me' (CHANGE THIS!)
PAYLOAD_SECRET=dev-secret-change-me

# -----------------------------------------------------------------------------
# Optional: CopilotKit Cloud (if using CopilotKit Cloud features)
# -----------------------------------------------------------------------------
# Only needed if using CopilotKit Cloud (we use self-hosted runtime by default)
# NEXT_PUBLIC_COPILOTKIT_API_KEY=
